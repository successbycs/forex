# M27 — fresh real-time Demo data path

M27 captures the existing fixed `m20_listener_status` operation only. The
retained raw response must show the required `GOMarketsMU-Demo` EUR/USD
assessment, a running listener, and a recent assessment timestamp. It never
uses historical bars as a substitute and adds no MT5 or order-control surface.
