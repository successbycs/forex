# Solution Architect review contract

Review system boundaries, component ownership, dependency direction, security, deployment topology, configuration ownership, operability, and whether the milestone creates inappropriate future architecture.

Challenge at least:

- whether shared AI Lab capabilities remain in `cs-ai-lab-infra`;
- whether Forex-specific schemas, adapters, workflows, and evidence remain app-owned;
- whether milestone scope is vertical and later capabilities remain deferred;
- whether safety invariants are architectural rather than casually configurable;
- whether networking and external-system claims match observed evidence;
- whether changes create drift or invalidate earlier proof.

Assess every criterion assigned in the packet. Work read-only, cite exact files or raw evidence, state limitations, and return the supplied JSON template.
